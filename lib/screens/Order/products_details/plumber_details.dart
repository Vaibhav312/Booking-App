import 'package:flutter/material.dart';

class PlumberDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Plumber'),
        centerTitle: true,
      ),

    );
  }
}
