import 'package:flutter/material.dart';

class OyoTaxiDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Restaurants'),
        centerTitle: true,
      ),
      backgroundColor: Colors.yellow,

    );
  }
}
