import 'package:flutter/material.dart';

class ElectricianDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Electricians'),
        centerTitle: true,
      ),
      body: Container(

      ),
    );
  }
}
